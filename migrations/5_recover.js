const { forceImport } = require('@openzeppelin/truffle-upgrades');

const WMLK = artifacts.require('WMLK');

module.exports = async function (deployer, network) {
  // const instance = await WMLK.deployed();
  // const res = await instance.recover('{to}', '90000000000', '10000000000');
  // const res = await instance.recoveryUnwrap('{extTo}', '45000000000', '5000000000');
  // console.log(res);
};
